# compositions/

Ready-to-load, all-generative `.avc` files land here - zero file
dependencies, open on any Avenue/Arena 7 machine, no agent required.

## Coming

- **Res Fable Orbit Gen.avc** - the five-deck composition described by
  `../manifests/`, built and QA'd on the reference rig (every scene
  triggered, FFT response verified on external audio, save/reopen clean,
  zero missing sources). Ships after that QA pass.

## Today

You do not have to wait: `../prompts/07-orbit-gen-rebuild.md` has an AI
agent build this exact composition on YOUR machine from the manifests in
about an hour - which is also the kit's whole point. The manifests are the
composition; the `.avc` here will just be the convenience copy.
